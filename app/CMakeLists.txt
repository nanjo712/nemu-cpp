add_executable(
    ${PROJECT_NAME}
    main.cpp
)
target_link_libraries(
    ${PROJECT_NAME}
    PRIVATE
    Utils
    Memory
    ISA_RISCV32
    ${Readline_LIBRARY}
    spdlog::spdlog_header_only
)
target_include_directories(
    ${PROJECT_NAME}
    PUBLIC
    ${NEMU_CPP_HOME}/include
)